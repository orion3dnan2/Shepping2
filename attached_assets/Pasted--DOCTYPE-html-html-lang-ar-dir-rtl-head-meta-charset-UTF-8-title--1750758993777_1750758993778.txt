<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <title>تتبع مستند</title>
  <style>
    body {
      font-family: 'Tajawal', sans-serif;
      background-color: #ffffff;
      margin: 0;
      padding: 30px;
    }
    h2 {
      color: #0f172a;
      margin-bottom: 30px;
    }
    #tracking_steps {
      display: flex;
      flex-wrap: nowrap;
      gap: 20px;
      overflow-x: auto;
      padding-bottom: 30px;
    }
    .step-item {
      flex: 0 0 auto;
      width: 180px;
      text-align: center;
      position: relative;
    }
    .step-circle {
      width: 50px;
      height: 50px;
      border-radius: 50%;
      background-color: #d1d5db;
      color: #000;
      font-weight: bold;
      line-height: 50px;
      margin: 0 auto 10px;
      transition: 0.3s;
    }
    .step-circle.active {
      background-color: #10b981;
      color: #fff;
    }
    .step-label {
      font-size: 14px;
      color: #334155;
    }
    .step-btn {
      margin-top: 10px;
      font-size: 12px;
      padding: 5px 10px;
      border: none;
      background-color: #475569;
      color: white;
      border-radius: 5px;
      cursor: pointer;
    }
    .step-btn:hover {
      background-color: #1e293b;
    }
  </style>
</head>
<body>

  <h2>📄 تتبع شحنة مستند</h2>

  <div id="tracking_steps"></div>

  <script>
    const documentSteps = [
      "تم استلام المستند",
      "تم إرسال المستند",
      "تم استلام المستند",
      "جاري التوثيق",
      "تم التوثيق",
      "جاري الإرسال بعد التوثيق",
      "تم الاستلام بعد التوثيق",
      "تم التسليم"
    ];

    const currentStatusIndex = 3; // يمكن تغييره حسب الحالة الحالية

    const container = document.getElementById("tracking_steps");
    container.innerHTML = "";

    documentSteps.forEach((step, idx) => {
      const div = document.createElement("div");
      div.className = "step-item";
      div.innerHTML = `
        <div class="step-circle ${idx <= currentStatusIndex ? 'active' : ''}">
          ${idx < currentStatusIndex ? '✔' : idx + 1}
        </div>
        <div class="step-label">${step}</div>
        <button class="step-btn" onclick="alert('تم اختيار: ${step}')">تحديث للحالة</button>
      `;
      container.appendChild(div);
    });
  </script>

</body>
</html>
